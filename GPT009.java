/*
 * Default (Package-private)
 * if no access modifier is specified, it defaults to package-private, which means the member is accessible within the same package.
 */

public class GPT009 {
  int packagePrivateVariable;
  void packagePrivateMethod() {
    // code here
  }
}
